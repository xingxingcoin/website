pipeline {
    agent any
    stages {
        stage('Install Deployer') {
            steps {
                steps {
                    sh script: "cd .deploy && composer install"
                }
            }
        }
        stage('Deploy Prod') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
