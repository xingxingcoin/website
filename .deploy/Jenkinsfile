pipeline {
    agent any
    stages {
        stage('Install Deployer') {
            steps {
                steps {
                    sh 'cd .deploy && composer install'
                }
            }
        }
        stage('Deploy Prod') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
