{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('meme-generator-infinite-scrolling') }}
    {{ encore_entry_script_tags('meme-generator-filter') }}
{% endblock %}


{% block content %}
    <div>
        <h1 class="title">{{ content.title }}</h1>
        <div class="description-container container-left-hidden">{{ content.description|raw }}</div>
    </div>

    {% include 'includes/blocks.html.twig' %}
{% endblock %}
