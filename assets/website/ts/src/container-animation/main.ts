import ContainerAnimationInitializer from '../components/ContainerAnimationInitializer';

document.addEventListener('DOMContentLoaded', function (): void {
    new ContainerAnimationInitializer().init();
});
